const BlogCard = (props) => {
    return(
        <div>
            <article>
                {switch(props.type){
                    case 'video':

                    break;
                    case 'still';

                    break;
                    case: 'gif';

                    break;
                    case 'text':
                }}
                <h1>
                    props.header
                </h1>
                <p>
                    props.caption
                </p>

            </article>
        </div>
    )
}